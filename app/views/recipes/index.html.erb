<h1>This page will show the search bar and results in lists of 10.</h1>


<%= form_tag("/", method: "get") do %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>

<%=@recipes%>
<%=params%>
